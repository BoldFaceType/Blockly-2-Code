// This file is used to configure Vitest with custom matchers for testing.

// Import the jest-dom library to extend Vitest's `expect` with useful DOM-related matchers.
// This allows for more readable and robust tests, for example: `expect(element).toBeInTheDocument()`
import '@testing-library/jest-dom';
